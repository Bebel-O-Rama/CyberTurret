using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HitInstance
{
    private HitType _hitType;
    private HitData _hitData;

    public HitInstance(HitType hitType, HitData hitData)
    {
        _hitType = hitType;
        _hitData = hitData;
    }
    
    public 
    
}
